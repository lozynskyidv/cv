# Job-specific tailored YAML files (optional - uncomment if you don't want to track)
# yaml/cv_*.yaml
# !cv_master.yaml  # Keep master tracked

# Typst intermediate files (for debugging only)
typ_files/

# Generated PDFs (optional - uncomment if you don't want to track resumes)
# resumes/*.pdf

# Python
__pycache__/
*.pyc
*.pyo

# macOS
.DS_Store

# Note: cv_master.yaml is tracked (source of truth)
# Note: yaml/cv_[job].yaml are tracked by default (tailoring history)
# Note: resumes/*.pdf can be tracked or ignored based on preference
