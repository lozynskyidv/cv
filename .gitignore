# Keep generated CVs for portfolio/reference
# output/*.pdf (commented out - we want to track PDFs)
# output/*.typ (keep these for debugging)

# Python
__pycache__/
*.pyc
*.pyo

# macOS
.DS_Store

# Keep the output directory structure
!output/.gitkeep
